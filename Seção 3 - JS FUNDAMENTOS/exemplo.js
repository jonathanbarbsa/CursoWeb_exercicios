// Objeto é definido como '{ }' 
// Array "lista", é definido como '[ ]'
// Função é definido como '( )'


/* Criando uma função soma */

const soma = function (numero1, numero2) {
    console.log(numero1 + numero2) // Caso você tente rodar o código do jeito que está aqui, não vai acontencer nada!
}                                  // Mas, por que? Uma função precisa ser utilizada, 
                                   //               podemos dizer que você criou uma função, mas não está a utilizando

/* Criando um Array 'lista' */

const lista = ['Fernanda', 'Maria', 'Nathalia', 'Ana'] /* Novamente, caso você tente rodar o código até aqui, não vai acontecer nada! */
                                                       // Até agora, criamos apenas variaveis! Uma contendo uma função, e outra uma lista!

/* Criando um Objeto! */

const funcionario = {
    nome: 'Paulo',
    salario: 2000.00,
    cargo: 'TI' 
}
